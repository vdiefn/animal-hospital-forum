<h4 class="fw-bold">使用者資訊</h4>
<div class="row mb-5 mt-5" >
  <img class="img-responsive center-block mb-5" src="{{user.image}}"
    style="width: 250px; height:230px; border: 1px solid #b1c0d8;" onload="this.style.opacity=1;">

  <div class="col-md-4 mt-3">
    <div class="d-flex">
      <h5>名稱：</h5><h4>{{user.name}}</h4>
    </div>
    <div class="d-flex">
      <div class="d-flex">
        <h5>Email：</h5>
        <h4>{{user.email}}</h4>
      </div>
    </div>
    <div class="d-flex justify-content-end mt-5">
      <a class="btn btn-primary" href="/users/{{user._id}}/edit">Edit</a>
    </div>
  </div>
  <hr>
</div>